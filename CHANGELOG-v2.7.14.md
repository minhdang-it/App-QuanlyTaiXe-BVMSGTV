# CHANGELOG v2.7.14

- Tối giản trình xem ảnh đính kèm: ảnh hiển thị trực tiếp, bỏ cột danh sách và khung preview lớn khi chỉ có một ảnh.
- Nhiều ảnh: hiển thị ảnh lớn và thanh thumbnail gọn bên dưới.
- PDF/Word/Excel/PowerPoint/tệp văn bản tiếp tục mở ở tab mới.
- Trường hợp vừa có ảnh vừa có văn bản: ảnh hiển thị trong app, văn bản nằm ở danh sách liên kết gọn phía dưới.
- Không thay đổi database hay nghiệp vụ.
